# .env.traefik.example
#
# Copy this file to .env and fill in your values before running:
#   docker compose -f docker-compose.traefik.yml up -d

# Email address used for Let's Encrypt certificate registration
ACME_EMAIL=your@example.com

# Fully-qualified domain name for the OpenClaw gateway
# Must point to this server's public IP address
OPENCLAW_DOMAIN=openclaw.example.com

# Fully-qualified domain name for the ClawMetry dashboard
# Must point to this server's public IP address
CLAWMETRY_DOMAIN=clawmetry.example.com

# Optional: your display name shown in the ClawMetry Flow tab
OPENCLAW_USER=
